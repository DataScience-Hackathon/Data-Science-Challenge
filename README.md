This file contains code which takes an training dataset analysed the values and does EDA on the dataset and then fills missing values and different models are  trained and validated on the training data and then the trained models are used to predict the missing values in test dataset which in this case is h1n1_vaccine where our model predicts if the person is gonna take the vaccine or not where 0=False 1=true and the results from different models is saved ascsv files and are also uploaded.
